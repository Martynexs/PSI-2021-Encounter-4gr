<?xml version="1.0" encoding="utf-8" ?>
<ContentPage  xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PSI.Views.Map"
             xmlns:vm="clr-namespace:Map3.ViewModels" xmlns:maps="clr-namespace:Xamarin.Forms.Maps;assembly=Xamarin.Forms.Maps" x:DataType="vm:MapViewModel">
    <ContentPage.Content>
        <Grid>

            <Grid.RowDefinitions>
                <RowDefinition Height = "auto" />
                <RowDefinition Height = "auto" />
                <RowDefinition Height = "*" />
            </Grid.RowDefinitions>

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width = "*" />
                <ColumnDefinition Width = "*" />
                <ColumnDefinition Width = "*" />
            </Grid.ColumnDefinitions>


            <StackLayout Grid.Row="0" Grid.ColumnSpan ="3" >
                <Entry Text = "{Binding Origin}" Placeholder="Origin" ></Entry>
                <Entry Text = "{Binding Destination}" Placeholder="Destination"></Entry>
                <Button Text= "Get route!" Command="{Binding GetRouteCommand}"></Button>

            </StackLayout>

            <ActivityIndicator HeightRequest = "10" Grid.Row="1" Grid.Column="0" IsRunning="{Binding IsBusy}"/>
            <Label Grid.Row="1" Grid.Column="1" Text="{Binding RouteDistance, StringFormat='{0} km'}"/>
            <Label Grid.Row="1" Grid.Column="2" Text="{Binding RouteDuration, StringFormat='{0} mins'}"/>
            <maps:Map IsShowingUser ="True" Grid.Row="2" Grid.ColumnSpan="3"  x:Name="map"/>
        </Grid>
    </ContentPage.Content>
</ContentPage>
