<?xml version="1.0" encoding="utf-8" ?>
<popup:PopupPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:popup="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 x:Class="PSI.Views.EditAnswerPopup"
                 BackgroundColor="White">

        <popup:PopupPage.Animation>
        <animations:ScaleAnimation 
                PositionIn="Center"
                PositionOut="Center"
                ScaleIn="1.2"
                ScaleOut="0.8"
                DurationIn="400"
                DurationOut="300"
                EasingIn="SinOut"
                EasingOut="SinIn"
                HasBackgroundAnimation="True"/>
    </popup:PopupPage.Animation>

    <ScrollView>

        <StackLayout Padding="0"
                     Margin="0">
            <Grid>

                <StackLayout Margin="15,20,15,20">
                    <Frame BackgroundColor="White"
                           HasShadow="True"
                           CornerRadius="2"
                           HorizontalOptions="FillAndExpand">
                        <StackLayout>

                            <StackLayout HorizontalOptions="Center" Padding="1,1,1,1">
                                <Label Text="Change Answer"
                                       TextColor="#FFC900"
                                       FontSize="Large"
                                       FontAttributes="Bold"/>
                            </StackLayout>

                            <StackLayout Margin="0,10,0,0">
                                <Label Text="Question:"
                                       TextColor="#FFC900"
                                       FontSize="Medium"/>

                                <Frame BackgroundColor="Transparent"
                                       BorderColor="Black"
                                       CornerRadius="50"
                                       HorizontalOptions="FillAndExpand"
                                       Padding="0" Margin="0">

                                    <StackLayout  VerticalOptions="Center" HorizontalOptions="FillAndExpand">
                                        <Entry Text="{Binding Answer}"/>
                                    </StackLayout>
                                </Frame>
                            </StackLayout>

                            <StackLayout Orientation="Horizontal">
                                <Label Text="True " TextColor="{Binding Color}" FontSize="Medium"/>
                                <CheckBox IsChecked="{Binding IsTrue}" Color="#FFC900" IsEnabled="{Binding AllowSelecting}"/>
                                <Label Text="    " TextColor="{Binding Color}" FontSize="Medium"/>
                                <Label Text="False " TextColor="{Binding Color}" FontSize="Medium"/>
                                <CheckBox IsChecked="{Binding IsFalse}" Color="#FFC900" IsEnabled="{Binding AllowSelecting}"/>
                            </StackLayout>

                            <StackLayout Orientation="Horizontal"
                                         HorizontalOptions="Center"
                                         Margin="0,20,0,0"
                                         HeightRequest="50">

                                <Button Text="Cancel"
                                        TextColor="Black"
                                        BackgroundColor="#FFC900"
                                        CornerRadius="30"
                                        Command="{Binding CancelCommand}"/>

                                <Button Text="Change"
                                        TextColor="Black"
                                        BackgroundColor="#FFC900"
                                        CornerRadius="30"
                                        Command="{Binding SaveCommand}"/>
                            </StackLayout>

                            <StackLayout Orientation="Horizontal"
                                         HorizontalOptions="Center"
                                         Margin="0,20,0,0"
                                         HeightRequest="50">

                                <Button Text="Delete answer"
                                        TextColor="Black"
                                        BackgroundColor="#FFC900"
                                        CornerRadius="30"
                                        Command="{Binding DeleteCommand}"/>
                            </StackLayout>

                        </StackLayout>
                    </Frame>
                </StackLayout>
            </Grid>
        </StackLayout>
    </ScrollView>
    </popup:PopupPage>